# @package _global_

# Clean-action prediction with AML-style velocity-space MSE.
defaults:
  - libero_uncond_2cam224_1e-4
  - _self_

model:
  action_prediction_type: x_prediction_v_loss
  action_x_pred_v_loss_eps: 0.05
